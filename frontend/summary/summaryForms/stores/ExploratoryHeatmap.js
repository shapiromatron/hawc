import {observable, action, computed} from "mobx";

class ExploratoryHeatmap {
    constructor(rootStore) {
        this.root = rootStore;
    }

    @action setInitialData(initial) {
        // todo - resume here.
    }
}

export default ExploratoryHeatmap;
